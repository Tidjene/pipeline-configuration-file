apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chart-backend.fullname" . }}-test-connection"
  labels:
    {{- include "chart-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chart-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
